import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
    container: {
        padding: 10
    },
    proverbItem: {
        padding: 10,
        borderBottomWidth: 1,
        borderBottomColor: '#333'
    },
    proverbText: {
        fontSize: 20,
        color: "#444"
    }
})